package interface_adapter.threads;

/**
 * Idk if we even need this because threads
 * has no user input other than clicking, which is handled by the actionlistener anyway.
 */
public class ThreadsState {
}
